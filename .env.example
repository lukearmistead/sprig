# Sprig Configuration
# Edit this file and save it as .env (without .example)

# REQUIRED: Your Teller application ID 
# Get from https://teller.io/settings/application after creating a developer account
APP_ID=your_teller_app_id_here

# Teller environment (leave as development for personal use)
ENVIRONMENT=development

# REQUIRED: Claude API key for automatic transaction categorization
# Get from https://console.anthropic.com
CLAUDE_API_KEY=your_claude_api_key_here

# Certificate file paths (download from https://teller.io/settings/certificates)
# Place certificate.pem and private_key.pem in the certs/ folder
CERT_PATH=certs/certificate.pem
KEY_PATH=certs/private_key.pem

# Database location (default location is fine for most users)
DATABASE_PATH=sprig.db

# Bank access tokens (AUTO-GENERATED - don't edit manually)
# These are created automatically when you run: python sprig.py auth
# ACCESS_TOKENS=will_be_filled_automatically

# Logging level for debugging (uncomment to enable)
# LOG_LEVEL=INFO
